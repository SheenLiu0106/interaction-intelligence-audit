.DS_Store
*.swp
*.swo
.vscode/
.idea/

# Guardrail: runtime audit records belong in the TARGET product repo, never here.
# Prevent a stray in-repo audit run from polluting this reusable skill.
# (Reusable scaffolds live under templates/ and stay tracked; docs/self-audit/ stays tracked.)
/AUDIT_BUGS.md
/changelog.md
/docs/interaction-audit/
